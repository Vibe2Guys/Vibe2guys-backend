spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/vibe2guys
    username: postgres
    password: postgres

app:
  jwt:
    secret: local-development-secret-key-local-development-secret-key
    refresh-token-hash-secret: local-refresh-token-hash-secret-local-refresh-token-hash-secret
